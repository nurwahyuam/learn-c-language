#include<stdio.h>

int main(){
  const int a = 100, b = 50, c = a + b;
  printf("a: %d, b:  %d\nTotal a + b = %d", a, b, c);
  return 0;
}

// Simbol Operator aritmatik
// +,-,*,/,%,--a,++a

// Simbol Operator Comparison
// ==, !=, >, <, >=, <=

// Simbol Operator Logical
// &&, ||, !